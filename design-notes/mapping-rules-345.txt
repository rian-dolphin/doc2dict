need to have functionality for footnotes by attribute
and e.g. values, null removal


map key, attribute dest

footnoteId['@id'] value is 'F8'

needs to match to


need stuff to map keys to harmonized keys
eh just try simple first